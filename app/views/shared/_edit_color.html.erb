<%= form_for k, :html => { :class => :form }, id: => color_form do |f| %>

    <% ['yellow', 'red'].each do |c| %>
        <%= c.radio_button "cols[#{c}]" %>
    <% end %>

<% end %>

<!--
    = form_tag movies_path, :method => :get, :id => :ratings_form do
  Include:
  - @all_ratings.each do |rating|
    = rating
    = check_box_tag "ratings[#{rating}]", '1', if params[:ratings]; @checked_ratings.include?(rating) else true end
  = submit_tag 'Refresh', :id => :ratings_submit
 -->
